import Poster from '../Poster'

function Row({ title, url }) {
    return (
        <div>
            Here lies thine Row
            {/* rowArray.map() builds out the <Poster data={data} /> */}
        </div>
    )
} 

export default Row